#pragma once
class dynamicpoint {
public:


	void getSignaturesData(int arr, int sizearray,string subjects[]) {
		for (int i = 0; i < sizearray; i++) {
			arr[x] = new int[12];
			std::cout << "--- Enter " << x + 1 << " student's grades " << std::endl;
			for (int j = 0; j < 12; j++)
			{
				std::cout << "Enter " << subjects[y] << " grades " << std::endl;
				std::cin >> arr[x][y];
			}
			;
		}
	};
}